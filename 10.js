// 화살표 함수
// ES6 신 문법
function add(x, y) {
    return x + y
}

// 1-1. 기본적인 화살표 함수
let areorwFunc01 = (x, y) => {
    return x + y
}

// 1-2. 한 줄로
let areorwFunc02 = (x, y) => x + y

//
function testFucn(x) {
  return x;  
}

let areorwFunc03 = (x) => x;